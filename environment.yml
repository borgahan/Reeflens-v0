name: sam3annotator
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - transformers==5.6.2
      - accelerate
      - fastapi>=0.110
      - uvicorn[standard]
      - opencv-python-headless
      - pillow
      - numpy
